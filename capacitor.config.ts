import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'simple_yband_recorder',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
